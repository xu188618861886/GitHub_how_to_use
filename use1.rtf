上传文件

cd 到需要上传代码的根目录  
  
初始化git：git init 


git add .( .为添加整个文件夹，也可以换成某个文件)


写更新记录   git commit -m "xxx"


链接远程仓库（git仓库）：git remote add origin https://github.com/xxx（xxx为你的地址)
查看           git remote  -v

执行pull命令：git pull origin master
（ 失败则执行： git pull --rebase origin master ）


git push -u origin master     //将本地项目更新到github项目上去





注意：
删除origin         git remote rm origin
显示remote  git remote -v
